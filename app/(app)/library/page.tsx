import Link from "next/link";
import { ArrowUpRight, BookOpen } from "lucide-react";
import { benchmarkCases, specialties } from "@/lib/domain/specialties";

export default function LibraryPage() { return <div className="mx-auto max-w-7xl px-5 pb-24 pt-12 lg:px-8"><p className="eyebrow">Curated core library</p><div className="mt-4 flex flex-wrap items-end justify-between gap-5"><h1 className="display max-w-3xl text-5xl leading-[.95] sm:text-6xl">Seven clinical lenses.<br/>One thoughtful practice bank.</h1><Link href="/practice/core" className="flex items-center gap-2 rounded-full bg-[#0b1f2a] px-5 py-3 text-sm font-semibold text-white">Start core practice <ArrowUpRight size={16}/></Link></div><section className="mt-14 grid gap-px bg-[#d9dfdc] sm:grid-cols-2 lg:grid-cols-4">{specialties.map((specialty, index) => <Link key={specialty.id} href={`/questions?specialty=${specialty.id}`} className="group min-h-52 bg-[#f7f5ef] p-6 transition hover:bg-white"><span className="text-sm text-[#60717a]">0{index + 1}</span><div className="mt-10 size-3 rounded-full" style={{ backgroundColor: specialty.color }}/><h2 className="display mt-4 text-3xl group-hover:underline">{specialty.name}</h2><p className="mt-2 text-sm leading-6 text-[#60717a]">{specialty.description}</p></Link>)}</section><section className="mt-16"><div className="flex items-center gap-2"><BookOpen size={18} className="text-[#2a9d8f]"/><p className="eyebrow">Benchmark cases</p></div><div className="mt-5 grid gap-5 lg:grid-cols-2">{benchmarkCases.map((study) => <article key={study.id} className="border border-[#d9dfdc] bg-white p-6"><p className="text-xs font-bold uppercase tracking-wider text-[#16756a]">{study.specialty.replaceAll("_", " ")}</p><h2 className="display mt-3 text-3xl">{study.title}</h2><p className="mt-3 leading-7 text-[#60717a]">{study.summary}</p><p className="mt-5 border-t border-[#e4e8e5] pt-4 text-sm leading-6"><strong>Rehab frame:</strong> {study.rehabPlan}</p></article>)}</div></section></div>; }
